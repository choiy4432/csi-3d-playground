{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "$json = $input | ConvertFrom-Json; $f = $json.tool_input.file_path; if ($f -match 'src[/\\\\].*\\.(jsx|js)$') { Set-Location 'c:\\Users\\choiy\\r3f_test\\csi-scene'; npx vitest run 2>&1; exit 0 }",
            "timeout": 60,
            "statusMessage": "vitest 실행 중..."
          }
        ]
      }
    ]
  }
}
